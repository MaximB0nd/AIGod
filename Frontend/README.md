# 🧠 AIgod — Мультиагентная симуляция с эмоциональным интеллектом

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
![React](https://img.shields.io/badge/React-18.3+-blue)
![Vite](https://img.shields.io/badge/Vite-6.0+-purple)
![TypeScript](https://img.shields.io/badge/TypeScript-5.0+-blue)

**AIgod** — веб-приложение, в котором AI-агенты живут своей жизнью: общаются, строят отношения, имеют память и эмоции. Пользователь создаёт комнаты, добавляет персонажей и наблюдает за их взаимодействием в реальном времени.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Структура проекта](#-структура-проекта)
- [Требования](#-требования)
- [Установка и запуск](#-установка-и-запуск)
- [Конфигурация](#-конфигурация)
- [Сборка для production](#-сборка-для-production)
- [Устранение неполадок](#-устранение-неполадок)

---

## ✨ Возможности

- **Комнаты и чаты** — создание комнат с разными режимами оркестрации (single, circular, narrator, full_context)
- **AI-агенты** — персонажи с характером, памятью, планами и настроением
- **Граф отношений** — интерактивная визуализация связей между агентами
- **Реальное время** — WebSocket для чата и обновлений графа
- **Авторизация** — регистрация и вход по email/паролю

---

## 📁 Структура проекта

```
AIGod/
├── Frontend/                 # React + Vite приложение
│   ├── src/
│   │   ├── api/             # API-клиент и эндпоинты
│   │   ├── components/      # UI-компоненты
│   │   ├── context/         # React Context (Auth, Chat)
│   │   ├── hooks/           # Кастомные хуки
│   │   ├── pages/           # Страницы приложения
│   │   └── types/           # TypeScript типы
│   ├── docs/                # Документация API
│   ├── .env.example         # Пример переменных окружения
│   ├── package.json
│   └── vite.config.ts
└── README.md
```

> **Примечание:** Бэкенд (FastAPI) — отдельный сервис. Фронтенд подключается к нему по URL, указанному в `VITE_API_URL`.

---

## 📦 Требования

| Компонент | Версия |
|-----------|--------|
| **Node.js** | 18+ (рекомендуется 20+) |
| **npm** | 9+ (или yarn, pnpm) |

Для работы приложения нужен **работающий бэкенд AIgod** (локально или на удалённом сервере).

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/ваш-username/AIGod.git
cd AIGod
```

### 2. Установка зависимостей фронтенда

```bash
cd Frontend
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в папке `Frontend` на основе примера:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите URL бэкенда:

```env
# URL бэкенда AIgod API
# Для локального бэкенда:
VITE_API_URL=http://localhost:8000

# Для удалённого сервера (пример):
# VITE_API_URL=http://5.39.250.179:8000
```

> **Режим разработки:** Vite проксирует запросы `/api/*` на бэкенд, указанный в `VITE_API_URL`. Это обходит CORS и 405 ошибки.

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется по адресу: **http://localhost:5173**

### 5. Проверка подключения к бэкенду

- Убедитесь, что бэкенд запущен и доступен по указанному URL
- Swagger UI бэкенда: `http://localhost:8000/docs` (или ваш URL + `/docs`)
- При первом запуске зарегистрируйтесь или войдите через интерфейс

---

## ⚙️ Конфигурация

### Переменные окружения (Frontend)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_API_URL` | URL бэкенда AIgod API | `http://5.39.250.179:8000` |

### Vite Proxy (режим dev)

В `vite.config.ts` настроен прокси: все запросы к `/api` перенаправляются на бэкенд. WebSocket-подключения (`/api/rooms/{id}/chat`, `/api/rooms/{id}/graph`) также проксируются.

---

## 📦 Сборка для production

### Сборка статических файлов

```bash
cd Frontend
npm run build
```

Результат сборки будет в папке `Frontend/dist/`.

### Просмотр production-сборки локально

```bash
npm run preview
```

### Деплой

1. Соберите проект: `npm run build`
2. Разместите содержимое `dist/` на веб-сервере (Nginx, Apache, Vercel, Netlify и т.д.)
3. **Важно:** Установите `VITE_API_URL` перед сборкой — он встраивается в бандл на этапе `npm run build`

Пример сборки с кастомным API:

```bash
VITE_API_URL=https://api.your-domain.com npm run build
```

---

## 🔧 Устранение неполадок

### Ошибка «Требуется авторизация» (401)

- Проверьте, что вы вошли в систему
- Токен хранится в `localStorage` — при смене домена или очистке данных потребуется повторный вход

### Бэкенд недоступен / CORS / 405

- В **dev-режиме** используйте Vite proxy — запросы идут через `localhost:5173`, CORS не применяется
- Убедитесь, что `VITE_API_URL` в `.env` указывает на работающий бэкенд
- Проверьте, что бэкенд разрешает ваш origin в CORS (для production)

### WebSocket не подключается

- WebSocket должен подключаться **до** отправки сообщений
- Проверьте, что бэкенд поддерживает WebSocket на путях `/api/rooms/{id}/chat` и `/api/rooms/{id}/graph`
- Токен передаётся в query: `?token=JWT`

### Порт 5173 занят

Vite автоматически попробует следующий свободный порт (5174, 5175 и т.д.). Или укажите порт явно:

```bash
npm run dev -- --port 3000
```

---

## 📚 Дополнительная документация

| Файл | Описание |
|------|----------|
| `Frontend/docs/API_DOCS.md` | Полная документация REST API и WebSocket |
| `Frontend/docs/ARCHITECTURE.md` | Архитектура фронтенда |
| `Frontend/docs/CONNECTION.md` | Подключение к бэкенду, CORS, примеры |

---

## 📜 Лицензия

MIT
