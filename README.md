# 🧠 AI Society Simulator
### Мультиагентная симуляция жизни с эмоциональным интеллектом

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
![Python](https://img.shields.io/badge/Python-3.11-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green)
![React](https://img.shields.io/badge/React-18.2+-blue)

**AI Society Simulator** — это веб-приложение, в котором несколько автономных AI-агентов живут своей жизнью. Они обладают личностью, памятью, настроением и строят отношения друг с другом. Пользователь может наблюдать за их жизнью в реальном времени и вмешиваться в неё.

## ✨ Возможности (Features)

### 🤖 Ядро агента
*   **Личность и память:** Каждый агент имеет уникальный характер, долговременную (векторную) и кратковременную память.
*   **Эмоциональный интеллект:** Модель настроения агента меняется в зависимости от происходящих событий и влияет на стиль его речи.
*   **Автономное планирование:** Агенты проходят цикл "Рефлексия → Постановка цели → Действие".
*   **Суммаризация:** При переполнении контекста старые воспоминания автоматически суммаризируются.

### 🤝 Взаимодействие
*   **Мультиагентность:** Агенты обмениваются сообщениями, спорят и дружат.
*   **Динамические отношения:** Формируются долгосрочные связи (симпатия/антипатия), влияющие на взаимодействие.

### 🖥 Веб-интерфейс (Дашборд)
*   **Лента событий:** Поток действий и диалогов агентов в реальном времени.
*   **Граф отношений:** Интерактивный граф (D3.js), где узлы — агенты, а цвет ребер отражает уровень симпатии.
*   **Инспектор агента:** Клик по агенту открывает профиль с его характером, ключевыми воспоминаниями и текущими планами.
*   **Панель управления:**
    *   Слайдер скорости времени.
    *   Отправка прямых сообщений агенту.
    *   Глобальные события (например, "Найден клад!").
*   **Страница агента:** Детальная история событий и взаимоотношений выбранного персонажа.

## 🛠 Технологический стек

### Frontend
*   **React** (или Vue/Svelte на выбор команды)
*   **D3.js** — для построения графа отношений
*   **Socket.IO-client** — для получения событий в реальном времени
*   **CSS Modules / Styled Components**

### Backend (LLM-ядро)
*   **FastAPI** — асинхронное ядро приложения
*   **Uvicorn** — ASGI-сервер
*   **Python 3.11+**

### ИИ и Память
*   **LLM-провайдер:** OpenAI / Google Gemini / YandexGPT (API)
*   **Векторная БД:** ChromaDB / Qdrant (для эпизодической памяти)
*   **Эмбеддинги:** Sentence-Transformers (`all-MiniLM-L6-v2`)

### Инфраструктура
*   **База данных:** PostgreSQL (для хранения статики: имена, аватарки) или SQLite для прототипа
*   **Реальное время:** WebSockets (via `python-socketio`)
*   **Контейнеризация:** Docker / Docker Compose

## 🚀 Быстрый старт (Запуск прототипа)

### Предварительные требования
*   Python 3.11+
*   Node.js 18+
*   Docker (опционально)
*   Ключ API (OpenAI/Gemini/Yandex)

### Установка и запуск

1.  **Клонировать репозиторий**
    ```bash
    git clone https://github.com/ваш-username/ai-society-simulator.git
    cd ai-society-simulator
    ```

2.  **Настройка бэкенда**
    ```bash
    cd backend
    python -m venv venv
    source venv/bin/activate  # На Windows: venv\Scripts\activate
    pip install -r requirements.txt
    ```
    Создайте файл `.env` в папке `backend` и укажите ключи API:
    ```env
    OPENAI_API_KEY=your_key_here
    # или GEMINI_API_KEY=...
    DATABASE_URL=sqlite:///./agents.db
    VECTOR_DB_PATH=./chroma_db
    ```

3.  **Запуск бэкенда**
    ```bash
    uvicorn main:app --reload --port 8000
    ```

4.  **Настройка фронтенда**
    ```bash
    cd ../frontend
    npm install
    ```
    При необходимости укажите адрес бэкенда в `.env` файле фронтенда:
    ```env
    REACT_APP_API_URL=http://localhost:8000
    ```

5.  **Запуск фронтенда**
    ```bash
    npm start
    ```

6.  Откройте браузер и перейдите по адресу: `http://localhost:3000`

### Запуск через Docker (альтернатива)
```bash
docker-compose up --build
```

---

## 📦 Развёртывание (Deployment)

Проект состоит из двух частей: **Python Backend** (запускается через Docker Compose) и **Frontend** (запускается через npm, без Docker).

---

### 🐳 Python Backend (Docker Compose)

Бэкенд разворачивается через Docker Compose **из корня проекта**.

#### Требования
- **Docker** и **Docker Compose**
- Ключи Yandex Cloud (YandexGPT API)

#### Шаги развёртывания

1. **Создайте файл `.env` в корне проекта** (или скопируйте из `python-backend/.env.example`):
   ```bash
   cp python-backend/.env.example .env
   ```

2. **Заполните переменные окружения в `.env`:**
   ```env
   # Yandex Cloud (обязательно для работы LLM)
   YANDEX_CLOUD_FOLDER=your_folder_id
   YANDEX_CLOUD_API_KEY=your_api_key

   # Лимит вызовов API в день — защита от перерасхода
   API_MESSAGE_LIMIT_PER_DAY=100

   # Опционально: JWT-секрет для production
   # SECRET_KEY=your-secret-key
   ```

3. **Запустите Docker Compose из корня:**
   ```bash
   docker-compose up --build
   ```

4. Бэкенд будет доступен по адресу: **http://localhost:8000**
   - Swagger UI: http://localhost:8000/docs

#### Зависимости и volumes
- Образ собирается из `python-backend/Dockerfile` (Python 3.13, FastAPI, ChromaDB, Yandex AI SDK)
- Данные (БД SQLite, ChromaDB) сохраняются в Docker volume `aigod_data`
- Переменные `SECRET_KEY`, `SQLITE_DB_PATH`, `API_MESSAGE_LIMIT_PER_DAY` передаются в контейнер

#### Остановка
```bash
docker-compose down
```

---

### 🖥 Frontend (npm)

Фронтенд **не использует Docker** — запускается через npm.

#### Требования
- **Node.js** 18+ (рекомендуется 20+)
- **npm** 9+
- Работающий бэкенд (локально или удалённо)

#### Шаги развёртывания

1. **Перейдите в папку Frontend:**
   ```bash
   cd Frontend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   ```
   Отредактируйте `.env` и укажите URL бэкенда:
   ```env
   # Для локального бэкенда:
   VITE_API_URL=http://localhost:8000

   # Для удалённого сервера:
   # VITE_API_URL=http://your-server:8000
   ```

4. **Запуск в режиме разработки:**
   ```bash
   npm run dev
   ```
   Приложение откроется по адресу: **http://localhost:5173**

5. **Сборка для production:**
   ```bash
   npm run build
   ```
   Результат в `Frontend/dist/`. Разместите содержимое на веб-сервере (Nginx, Vercel, Netlify и т.д.).

   **Важно:** `VITE_API_URL` встраивается в бандл при сборке. Для production:
   ```bash
   VITE_API_URL=https://api.your-domain.com npm run build
   ```

#### Vite Proxy (режим dev)
В dev-режиме Vite проксирует запросы `/api/*` на бэкенд — CORS и 405 обходятся автоматически. WebSocket (`/api/rooms/{id}/chat`, `/api/rooms/{id}/graph`) также проксируется.

#### Устранение неполадок
- **401 «Требуется авторизация»** — проверьте вход в систему, токен в `localStorage`
- **Бэкенд недоступен** — убедитесь, что `VITE_API_URL` указывает на работающий бэкенд
- **WebSocket не подключается** — проверьте, что бэкенд поддерживает WebSocket, токен передаётся в query `?token=JWT`
- **Порт 5173 занят** — укажите другой: `npm run dev -- --port 3000`

Подробнее: [Frontend/README.md](Frontend/README.md)
