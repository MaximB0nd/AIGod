# 🧠 AI Society Simulator
### Мультиагентная симуляция жизни с эмоциональным интеллектом

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
![Python](https://img.shields.io/badge/Python-3.11-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green)
![React](https://img.shields.io/badge/React-18.2+-blue)

**AI Society Simulator** — это веб-приложение, в котором несколько автономных AI-агентов живут своей жизнью. Они обладают личностью, памятью, настроением и строят отношения друг с другом. Пользователь может наблюдать за их жизнью в реальном времени и вмешиваться в неё.

## ✨ Возможности (Features)

### 🤖 Ядро агента
*   **Личность и память:** Каждый агент имеет уникальный характер, долговременную (векторную) и кратковременную память.
*   **Эмоциональный интеллект:** Модель настроения агента меняется в зависимости от происходящих событий и влияет на стиль его речи.
*   **Автономное планирование:** Агенты проходят цикл "Рефлексия → Постановка цели → Действие".
*   **Суммаризация:** При переполнении контекста старые воспоминания автоматически суммаризируются.

### 🤝 Взаимодействие
*   **Мультиагентность:** Агенты обмениваются сообщениями, спорят и дружат.
*   **Динамические отношения:** Формируются долгосрочные связи (симпатия/антипатия), влияющие на взаимодействие.

### 🖥 Веб-интерфейс (Дашборд)
*   **Лента событий:** Поток действий и диалогов агентов в реальном времени.
*   **Граф отношений:** Интерактивный граф (D3.js), где узлы — агенты, а цвет ребер отражает уровень симпатии.
*   **Инспектор агента:** Клик по агенту открывает профиль с его характером, ключевыми воспоминаниями и текущими планами.
*   **Панель управления:**
    *   Слайдер скорости времени.
    *   Отправка прямых сообщений агенту.
    *   Глобальные события (например, "Найден клад!").
*   **Страница агента:** Детальная история событий и взаимоотношений выбранного персонажа.

## 🛠 Технологический стек

### Frontend
*   **React** (или Vue/Svelte на выбор команды)
*   **D3.js** — для построения графа отношений
*   **Socket.IO-client** — для получения событий в реальном времени
*   **CSS Modules / Styled Components**

### Backend (LLM-ядро)
*   **FastAPI** — асинхронное ядро приложения
*   **Uvicorn** — ASGI-сервер
*   **Python 3.11+**

### ИИ и Память
*   **LLM-провайдер:** OpenAI / Google Gemini / YandexGPT (API)
*   **Векторная БД:** ChromaDB / Qdrant (для эпизодической памяти)
*   **Эмбеддинги:** Sentence-Transformers (`all-MiniLM-L6-v2`)

### Инфраструктура
*   **База данных:** PostgreSQL (для хранения статики: имена, аватарки) или SQLite для прототипа
*   **Реальное время:** WebSockets (via `python-socketio`)
*   **Контейнеризация:** Docker / Docker Compose

## 🚀 Быстрый старт (Запуск прототипа)

### Предварительные требования
*   Python 3.11+
*   Node.js 18+
*   Docker (опционально)
*   Ключ API (OpenAI/Gemini/Yandex)

### Установка и запуск

1.  **Клонировать репозиторий**
    ```bash
    git clone https://github.com/ваш-username/ai-society-simulator.git
    cd ai-society-simulator
    ```

2.  **Настройка бэкенда**
    ```bash
    cd backend
    python -m venv venv
    source venv/bin/activate  # На Windows: venv\Scripts\activate
    pip install -r requirements.txt
    ```
    Создайте файл `.env` в папке `backend` и укажите ключи API:
    ```env
    OPENAI_API_KEY=your_key_here
    # или GEMINI_API_KEY=...
    DATABASE_URL=sqlite:///./agents.db
    VECTOR_DB_PATH=./chroma_db
    ```

3.  **Запуск бэкенда**
    ```bash
    uvicorn main:app --reload --port 8000
    ```

4.  **Настройка фронтенда**
    ```bash
    cd ../frontend
    npm install
    ```
    При необходимости укажите адрес бэкенда в `.env` файле фронтенда:
    ```env
    REACT_APP_API_URL=http://localhost:8000
    ```

5.  **Запуск фронтенда**
    ```bash
    npm start
    ```

6.  Откройте браузер и перейдите по адресу: `http://localhost:3000`

### Запуск через Docker (альтернатива)
```bash
docker-compose up --build
