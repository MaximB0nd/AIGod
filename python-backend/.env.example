# Yandex Cloud (обязательно)
YANDEX_CLOUD_FOLDER=your_folder_id
YANDEX_CLOUD_API_KEY=your_api_key

# Лимит вызовов API в день — ОБЯЗАТЕЛЬНО для защиты от перерасхода
# Без лимита счёт может уйти в бесконечность
API_MESSAGE_LIMIT_PER_DAY=100

# БД (опционально)
# SQLITE_DB_PATH=aigod.db

# JWT (опционально)
# SECRET_KEY=your-secret-key
